{
  "name": "@cannaroute/compliance-service",
  "version": "0.1.0",
  "description": "CannaRoute Compliance Service — state rules engine, license validation, purchase limits, manifest generation",
  "main": "src/main.ts",
  "scripts": {
    "start": "nest start",
    "start:dev": "nest start --watch",
    "build": "nest build",
    "test": "jest"
  },
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "axios": "^1.5.0",
    "pdfmake": "^0.2.0",
    "class-validator": "^0.14.0"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
